apply plugin: "war"

bootWar {
    mainClassName = "jp.invastsec.tafx.trade.TafxtradeApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
